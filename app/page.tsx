// ROUTE SKELETON — homepage — replace in HILITOS-P1A-AMARILLO (owner: Amarillo).
// Inert placeholder only: no real content, no data fetching, no components.
export default function HomeSkeletonPage() {
  return (
    <main>
      <p>⟨ROUTE SKELETON — / (homepage) — se implementa en HILITOS-P1A-AMARILLO⟩</p>
    </main>
  );
}
